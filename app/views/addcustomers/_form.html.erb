<%= form_with(model: addcustomer) do |form| %>
  <% if addcustomer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(addcustomer.errors.count, "error") %> prohibited this addcustomer from being saved:</h2>

      <ul>
        <% addcustomer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :lastname %>
    <%= form.text_field :lastname %>
  </div>

  <div class="field">
    <%= form.label :homeaddress %>
    <%= form.text_area :homeaddress %>
  </div>

  <div class="field">
    <%= form.label :dateofbirth %>
    <%= form.date_select :dateofbirth %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
